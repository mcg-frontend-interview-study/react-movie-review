import styled from '@emotion/styled';

export const Portal = styled.div`
  pointer-events: 'auto';

  position: fixed;
  z-index: 10;
  top: 0;
  left: 0;

  display: block;

  width: 100%;
  height: 100%;
`;
