import {Header} from '@components/Header';
import {ItemView} from '@components/ItemView';

export const Main = () => {
  return (
    <div id="app">
      <Header />
      <ItemView />
    </div>
  );
};
